curl -w "\nTime to first byte: %{time_starttransfer}s\nTotal time: %{time_total}s\n" -N -X POST http://localhost:3000/api/workflows/ai -H "Content-Type: application/json" -d '{"prompt": "Build a very complex workflow that triggers on an order webhook, checks inventory via HTTP request, uses AI to write a personalized thank you email, generates a custom AI receipt image, runs a custom JS script to format the data, and finally sends the data to another HTTP endpoint.", "activeNodes": []}'
